<view class="container">
    <image class="icon" bind:tap="onLeft" src="{{lastest?leftDisImg:leftImg}}"/>
    <text class="title">{{title}}</text>
    <image class="icon" bind:tap="onRight" src="{{first?rightDisImg:rightImg}}"/>
</view>